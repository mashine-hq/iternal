= simple_form_for @category, url: [:admin, @category] do |f|
  = f.input :title
  = f.input :phone
  = f.input :description
  .form-group
    = f.file_field :logo, class: 'btn btn-default'
    = f.hidden_field :logo_cache
  = f.button :submit, 'Save', class: 'btn-primary'
