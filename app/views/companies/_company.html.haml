.row
  .col-xs-12
    %a{href: company_path(company)}
      = image_tag(company.logo.thumb.url, class: 'img-thumbnail')
      .title 
        = company.title
      %p.description
        = company.description
      .phones
        - company.phones.each do |phone|
          %a.phone{href: 'tel:'+phone}
            = number_to_phone(phone)