.navbar.navbar-inverse.navbar-fixed-top.gradient{role: 'navigation'}
  .container
    %a.navbar-left{href: category_path(@company.category)}
      .glyphicon.glyphicon-arrow-left
    %a.navbar-right{href: root_url}
      .glyphicon.glyphicon-home
.company
  .row
    .col-xs-12
      = image_tag(@company.logo.thumb.url, class: 'img-thumbnail')
      .title= @company.title
  - @company.phones.each do |phone|
    .row
      .col-xs-12
        %a.sms
        %a.call{href: 'tel:'+phone}
        .text-center.phone= number_to_phone(phone)
  .row.description
    .col-xs-12
      %h1 ОПИСАНИЕ СЛУЖБЫ
      %p= @company.description
    .text-center
      = mail_to('support@skylogic.com.ua') do
        %button.feedback.col-xs-12.btn.btn-default Написать разработчикам
