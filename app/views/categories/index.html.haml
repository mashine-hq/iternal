.categories
  .row.text-center
    - @categories.each do |category|
      .col-xs-4= link_to(image_tag(category.logo.thumb.url), category_path(category))
  .row.text-center
    .col-xs-4= link_to(image_tag('services/102.png'), 'tel:102')
    .col-xs-4= link_to(image_tag('services/103.png'), 'tel:103')
    .col-xs-4= link_to(image_tag('services/112.png'), 'tel:112')
