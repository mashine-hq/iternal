.categories
  - @categories.in_groups_of(3).each do |group|
    .row.text-center
      - group.each do |category|
        .col-xs-4
          = link_to(category_path(category)) do
            = image_tag(category.logo.thumb.url)
            .title= category.title
  .row.text-center
    .col-xs-4
      = link_to('tel:102') do
        = image_tag('services/102.png')
        .title Милиция
    .col-xs-4
      = link_to('tel:103') do
        = image_tag('services/103.png')
        .title Скорая помощь
    .col-xs-4
      = link_to('tel:112') do
        = image_tag('services/112.png')
        .title Служба спасения
